import { MOCK_POKEMON } from '../mock/pokemonMockData'; import { PokemonGrid } from '../components/PokemonGrid'; import { EmptyState } from '../components/EmptyState'; import './pages.css';
interface Props { favorites:string[]; comparison:string[]; onToggleFavorite:(name:string)=>void; onToggleComparison:(name:string)=>void; }
export function Favorites(props:Props){const pokemon=MOCK_POKEMON.filter(p=>props.favorites.includes(p.name));return <main className="page subpage"><p className="eyebrow">Your collection</p><h1>Your Favorites</h1><p className="subpage__lead">{pokemon.length} Pokémon collected</p>{pokemon.length?<PokemonGrid pokemon={pokemon} {...props}/>:<EmptyState title="Your Pokédex is waiting." text="Favorite Pokémon to build your collection." action/>}</main>}
