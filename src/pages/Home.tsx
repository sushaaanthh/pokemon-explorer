import { useMemo, useState } from 'react';
import type { PokemonTypeName, SortOption } from '../types/pokemon';
import { MOCK_POKEMON } from '../mock/pokemonMockData';
import { SearchBar } from '../components/SearchBar'; import { TypeFilter } from '../components/TypeFilter'; import { SortSelect } from '../components/SortSelect'; import { PokemonGrid } from '../components/PokemonGrid'; import { FeaturedPokemon } from '../components/FeaturedPokemon'; import { EmptyState } from '../components/EmptyState'; import './pages.css';
interface Props { favorites: string[]; comparison: string[]; onToggleFavorite: (name:string)=>void; onToggleComparison:(name:string)=>void; }
export function Home(props: Props) { const [query,setQuery]=useState(''); const [type,setType]=useState<PokemonTypeName|null>(null); const [sort,setSort]=useState<SortOption>('id'); const [shown,setShown]=useState(8); const pokemon=useMemo(()=>MOCK_POKEMON.filter(p=>(!type||p.types.some(t=>t.type.name===type))&&p.name.includes(query.toLowerCase())).sort((a,b)=>sort==='name'?a.name.localeCompare(b.name):sort==='id'?a.id-b.id:(b.stats.find(s=>s.stat.name===sort)?.base_stat??0)-(a.stats.find(s=>s.stat.name===sort)?.base_stat??0)),[query,type,sort]); const pikachu=MOCK_POKEMON.find(p=>p.name==='pikachu')!; return <main className="page"><section className="hero"><div className="hero__copy"><p className="eyebrow">Your digital Pokédex</p><h1>Discover your<br/>next favorite.</h1><p className="hero__text">Explore Pokémon, inspect their stats, filter by type, and build your own collection.</p><SearchBar onSearch={setQuery} onClear={()=>setQuery('')} /></div><FeaturedPokemon pokemon={pikachu}/></section><section className="browse"><div className="section-head"><div><p className="eyebrow">Explore the Pokédex</p><h2>Discover Pokémon</h2></div><div className="browse__controls"><span>1,025 results</span><SortSelect value={sort} onChange={setSort}/></div></div><TypeFilter selected={type} onSelect={setType}/><div className="grid-wrap">{pokemon.length?<PokemonGrid pokemon={pokemon.slice(0,shown)} {...props}/>:<EmptyState/>}</div>{shown<pokemon.length&&<button className="load-more" onClick={()=>setShown(value=>value+4)}>Load More</button>}</section></main>; }
